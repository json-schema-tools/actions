import:
  - repository: json-schema-tools/foundation # this is myself??
    path: '.circleci/setup.yaml'

steps:
  <<: *steps
  - run: npm install codecov
  - run: npm test
  - run: ./node_modules/.bin/codecov
